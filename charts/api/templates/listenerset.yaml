apiVersion: gateway.networking.k8s.io/v1
kind: ListenerSet
metadata:
  name: api
  namespace: {{ .Values.namespace }}
  annotations:
    cert-manager.io/cluster-issuer: {{ .Values.certIssuer.name }}
    {{- if ne .Values.certIssuer.name "selfsigned" }}
    acme.cert-manager.io/http01-parentreffallback: "true"
    {{- end }}
spec:
  parentRef:
    name: gateway
    namespace: envoy-gateway-system
  listeners:
    - name: api
      hostname: {{ .Values.hostname }}
      port: 443
      protocol: HTTPS
      tls:
        mode: Terminate
        certificateRefs:
          - name: api-tls
      allowedRoutes:
        namespaces:
          from: Same     
